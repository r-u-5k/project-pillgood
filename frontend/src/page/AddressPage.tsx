import { AddressContent } from "@/component/user/AddressContent";
import { DefaultLayout } from "../component/layout/Layout";

export const AddressPage = () => {
  return (
    <DefaultLayout>
      <AddressContent />
    </DefaultLayout>
  );
};
